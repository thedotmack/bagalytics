# Solana RPC endpoint (required for Jupiter limit orders)
# Get one from Chainstack, Helius, QuickNode, etc.
SOLANA_RPC_URL=https://your-solana-rpc-endpoint.com

# Bags.fm API key (required for lifetime fees)
# Get one from https://bags.fm
BAGS_API_KEY=your-bags-api-key

# Redis URL for caching (optional - falls back to in-memory cache)
REDIS_URL=redis://localhost:6379
